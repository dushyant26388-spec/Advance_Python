# 7. Calculate total fare collected from each passenger class
df.groupby("Passenger_Class")["Ticket_Price"].sum()

# 8. Find survival count by gender
df.groupby("Sex")["Survived"].sum()

# 9. Find average fare by gender
df.groupby("Sex")["Ticket_Price"].mean()

# 10. Generate a report showing:
# Count
# Mean
# Median
# Minimum
# Maximum
# Standard Deviation
# for ticket price grouped by passenger class.

df.groupby("Passenger_Class")["Ticket_Price"].agg(
    ["count","mean","median","min","max","std"])

# Part H: Merge Operations (20 Marks)

# Create the following DataFrames.

import pandas as pd
# Passenger Insurance
insurance = pd.DataFrame({
"PassengerId":[1,2,3,5,8,900],
"Policy":["Gold","Silver","Gold","Premium","Silver","Gold"]
})

# Passenger Meal Preference
meal = pd.DataFrame({
"PassengerId":[1,2,4,5,7,900],
"Meal":["Veg","Non-Veg","Veg","Veg","Non-Veg","Veg"]
})

# 1. Perform an Inner Merge between Titanic and Insurance.
inner_df=pd.merge(df,insurance,on="PassengerId",how="inner")
inner_df

# 2. Perform a Left Merge between Titanic and Insurance.
left_df=pd.merge(df,insurance,on="PassengerId",how="left")
left_df

# 3. Perform a Right Merge between Titanic and Insurance.
right_df=pd.merge(df,insurance,on="PassengerId",how="right")
right_df

# 4. Perform an Outer Merge between Titanic and Insurance.
outer_df=pd.merge(df,insurance,on="PassengerId",how="outer")
outer_df


# 5. Identify passengers who do not have insurance
left_df1=pd.merge(df,insurance,on="PassengerId",how="left")
left_df1[left_df1["Policy"].isnull()]

# 6. Identify insurance records that do not have matching passengers.
right_df1=pd.merge(df,insurance,on="PassengerId",how="right")
right_df1[right_df1["Name"].isnull()]

# 7. Merge Passenger, Insurance, and Meal datasets
final_df=pd.merge(df,insurance,on="PassengerId",how="left")
final_df1=pd.merge(final_df,meal,on="PassengerId",how="left")
final_df1

# 8. Replace missing insurance values
final_df1["Policy"].fillna("Not insured")

# 9. Replace missing meal value
final_df1["Meal"].fillna("meal not selected")

# 10. Save the merged dataset
final_df1.to_csv("Titanic_Passenger_Report.csv",index=False)